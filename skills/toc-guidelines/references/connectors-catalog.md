# Danh mục Connectors được hỗ trợ

> Danh mục connectors mà ClawExpert hỗ trợ (nội dung tĩnh, đóng gói sẵn trong skill). Tổng: 49 connector.
> Connector phơi ra cho agent qua MCP `tryopenclaw-connectors` (tool dạng `<APP>_<ACTION>`).
> Kiểm tra đã kết nối chưa bằng `tools/list` của MCP; kết nối mới qua giao diện ClawExpert (mục Connectors).

## Giao tiếp

### Discord ⭐  (`discord`)

- **Dùng để làm gì**: Discord là ứng dụng chat cộng đồng theo kênh. Với kết nối này, Agent có thể xem tin nhắn mới, đăng thông báo lên kênh, tìm tin và tóm tắt kênh trong server của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @discord xem tin nhắn mới trong server của mình."
- **Gọi nhanh**: gõ `@discord` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Discord → đăng nhập/cấp quyền.

### Facebook ⭐  (`facebook`)

- **Dùng để làm gì**: Facebook là mạng xã hội kết nối và chia sẻ nội dung. Với kết nối này, Agent có thể đăng bài, theo dõi tương tác, đọc bình luận và xem chỉ số trang của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @facebook xem tương tác bài đăng gần đây của mình."
- **Gọi nhanh**: gõ `@facebook` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Facebook → đăng nhập/cấp quyền.

### Gmail ⭐  (`gmail`)

- **Dùng để làm gì**: Gmail là dịch vụ email của Google. Với kết nối này, Agent có thể đọc, gửi, sắp xếp và tìm kiếm thư trong hộp thư của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @gmail kiểm tra mail chưa đọc của mình."
- **Gọi nhanh**: gõ `@gmail` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Gmail → đăng nhập/cấp quyền.

### LinkedIn ⭐  (`linkedin`)

- **Dùng để làm gì**: LinkedIn là mạng xã hội nghề nghiệp. Với kết nối này, Agent có thể tìm người, kết nối, tìm ứng viên và đăng bài cho cá nhân hoặc công ty.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @linkedin tìm ứng viên cho [vị trí cần tuyển]."
- **Gọi nhanh**: gõ `@linkedin` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn LinkedIn → đăng nhập/cấp quyền.

### Mailchimp ⭐  (`mailchimp`)

- **Dùng để làm gì**: Mailchimp là nền tảng email marketing. Với kết nối này, Agent có thể tạo danh sách, thêm khách vào audience, gửi chiến dịch và xem số liệu mở/click.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @mailchimp xem hiệu quả chiến dịch gần đây."
- **Gọi nhanh**: gõ `@mailchimp` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Mailchimp → đăng nhập/cấp quyền.

### Microsoft Teams ⭐  (`microsoftteams`)

- **Dùng để làm gì**: Microsoft Teams là ứng dụng chat đội nhóm của Microsoft. Với kết nối này, Agent có thể gửi tin vào kênh, tìm tin nhắn và tóm tắt hội thoại.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @microsoftteams tóm tắt tin nhắn mới của mình."
- **Gọi nhanh**: gõ `@microsoftteams` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Microsoft Teams → đăng nhập/cấp quyền.

### Outlook ⭐  (`outlook`)

- **Dùng để làm gì**: Outlook là dịch vụ email của Microsoft. Với kết nối này, Agent có thể đọc, soạn, gửi và sắp xếp thư trong hộp thư của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @outlook kiểm tra mail chưa đọc của mình."
- **Gọi nhanh**: gõ `@outlook` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Outlook → đăng nhập/cấp quyền.

### SendGrid ⭐  (`sendgrid`)

- **Dùng để làm gì**: SendGrid là nền tảng gửi email tự động cho từng giao dịch. Với kết nối này, Agent có thể gửi mail xác nhận đơn, mã OTP và đặt lại mật khẩu cho khách của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @sendgrid xem thống kê mail đã gửi gần đây."
- **Gọi nhanh**: gõ `@sendgrid` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn SendGrid → đăng nhập/cấp quyền.

### Slack ⭐  (`slack`)

- **Dùng để làm gì**: Slack là ứng dụng chat cho đội nhóm. Với kết nối này, Agent có thể đăng tin lên kênh, tìm tin nhắn, tóm tắt kênh và chia sẻ file cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @slack tóm tắt tin nhắn mới của mình."
- **Gọi nhanh**: gõ `@slack` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Slack → đăng nhập/cấp quyền.

### Twilio ⭐  (`twilio`)

- **Dùng để làm gì**: Twilio là nền tảng gửi SMS và gọi điện tự động. Với kết nối này, Agent có thể gửi SMS, mã OTP, tin xác nhận và thực hiện cuộc gọi tự động đến người nhận.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @twilio gửi SMS cho [số điện thoại]."
- **Gọi nhanh**: gõ `@twilio` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Twilio → đăng nhập/cấp quyền.

### Twitter / X ⭐  (`twitter`)

- **Dùng để làm gì**: X (Twitter) là mạng xã hội đăng tin ngắn. Với kết nối này, Agent có thể đăng bài, theo dõi tương tác, nhắn tin riêng cho người theo dõi và xem chỉ số bài viết.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @twitter xem tương tác bài đăng gần đây của mình."
- **Gọi nhanh**: gõ `@twitter` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Twitter / X → đăng nhập/cấp quyền.

### WhatsApp ⭐  (`whatsapp`)

- **Dùng để làm gì**: WhatsApp là ứng dụng nhắn tin phổ biến. Với kết nối này, Agent có thể gửi và trả lời tin nhắn cho khách, gửi tin xác nhận và nhắn vào nhóm.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @whatsapp xem tin nhắn khách gần đây."
- **Gọi nhanh**: gõ `@whatsapp` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn WhatsApp → đăng nhập/cấp quyền.

## Tài liệu

### Airtable ⭐  (`airtable`)

- **Dùng để làm gì**: Airtable là cơ sở dữ liệu kết hợp bảng tính. Với kết nối này, Agent có thể xem base, tìm record theo điều kiện, thêm và cập nhật dữ liệu trong bảng của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @airtable liệt kê các base gần đây của mình."
- **Gọi nhanh**: gõ `@airtable` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Airtable → đăng nhập/cấp quyền.

### Confluence ⭐  (`confluence`)

- **Dùng để làm gì**: Confluence là wiki tài liệu nội bộ của Atlassian. Với kết nối này, Agent có thể tìm trang theo chủ đề, tạo trang mới, cập nhật nội dung và tra cứu tài liệu của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @confluence liệt kê các trang gần đây trong wiki của mình."
- **Gọi nhanh**: gõ `@confluence` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Confluence → đăng nhập/cấp quyền.

### Google Docs ⭐  (`googledocs`)

- **Dùng để làm gì**: Google Docs là trình soạn thảo tài liệu của Google. Với kết nối này, Agent có thể mở, đọc, soạn nội dung mới và thêm comment vào tài liệu cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @googledocs liệt kê tài liệu gần đây của mình."
- **Gọi nhanh**: gõ `@googledocs` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Google Docs → đăng nhập/cấp quyền.

### Google Sheets ⭐  (`googlesheets`)

- **Dùng để làm gì**: Google Sheets là bảng tính online của Google. Với kết nối này, Agent có thể đọc dữ liệu, thêm dòng, cập nhật ô và lọc theo điều kiện cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @googlesheets liệt kê bảng tính gần đây của mình."
- **Gọi nhanh**: gõ `@googlesheets` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Google Sheets → đăng nhập/cấp quyền.

### Notion ⭐  (`notion`)

- **Dùng để làm gì**: Notion là không gian làm việc all-in-one cho ghi chú, tài liệu và wiki. Với kết nối này, Agent có thể tìm trang, tạo doc và cập nhật mục trong workspace.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @notion liệt kê các trang gần đây của mình."
- **Gọi nhanh**: gõ `@notion` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Notion → đăng nhập/cấp quyền.

## CRM & Bán hàng

### Google Analytics ⭐  (`google_analytics`)

- **Dùng để làm gì**: Google Analytics là công cụ đo lường lưu lượng truy cập website. Với kết nối này, Agent có thể xem lượt truy cập, theo dõi nguồn traffic, tra số liệu chuyển đổi và tổng hợp báo cáo cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @google_analytics xem lượt truy cập gần đây của mình."
- **Gọi nhanh**: gõ `@google_analytics` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Google Analytics → đăng nhập/cấp quyền.

### HubSpot ⭐  (`hubspot`)

- **Dùng để làm gì**: HubSpot là nền tảng CRM cho marketing, bán hàng và chăm sóc khách. Với kết nối này, Agent có thể thêm lead, cập nhật deal, xem pipeline và tra hồ sơ contact cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @hubspot xem các lead mới gần đây."
- **Gọi nhanh**: gõ `@hubspot` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn HubSpot → đăng nhập/cấp quyền.

### Pipedrive ⭐  (`pipedrive`)

- **Dùng để làm gì**: Pipedrive là phần mềm CRM bán hàng đơn giản. Với kết nối này, Agent có thể xem, thêm, cập nhật và theo dõi deal trên pipeline của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @pipedrive xem các deal trong pipeline của mình."
- **Gọi nhanh**: gõ `@pipedrive` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Pipedrive → đăng nhập/cấp quyền.

### Salesforce ⭐  (`salesforce`)

- **Dùng để làm gì**: Salesforce là nền tảng CRM cho doanh nghiệp. Với kết nối này, Agent có thể xem, thêm, cập nhật opportunity và lead cùng xem báo cáo cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @salesforce xem các cơ hội bán hàng gần đây."
- **Gọi nhanh**: gõ `@salesforce` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Salesforce → đăng nhập/cấp quyền.

### Zoho ⭐  (`zoho`)

- **Dùng để làm gì**: Zoho là bộ công cụ CRM và văn phòng cho doanh nghiệp. Với kết nối này, Agent có thể thêm lead mới, cập nhật deal, xem báo cáo và quản lý hồ sơ khách hàng.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @zoho xem các lead mới gần đây."
- **Gọi nhanh**: gõ `@zoho` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Zoho → đăng nhập/cấp quyền.

## Năng suất

### Asana ⭐  (`asana`)

- **Dùng để làm gì**: Asana là công cụ quản lý dự án và công việc. Với kết nối này, Agent có thể xem task được giao, tạo việc mới, đặt deadline và theo dõi tiến độ của team.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @asana xem các task giao cho mình."
- **Gọi nhanh**: gõ `@asana` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Asana → đăng nhập/cấp quyền.

### ClickUp ⭐  (`clickup`)

- **Dùng để làm gì**: ClickUp là nền tảng quản lý dự án all-in-one. Với kết nối này, Agent có thể xem task được giao, tạo việc mới, cập nhật trạng thái và theo dõi dự án của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @clickup xem các task giao cho mình."
- **Gọi nhanh**: gõ `@clickup` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn ClickUp → đăng nhập/cấp quyền.

### Lark Suite ⭐  (`larksuite`)

- **Dùng để làm gì**: Lark Suite là bộ công cụ làm việc all-in-one. Với kết nối này, Agent có thể xem tin nhắn, gửi tin vào nhóm, tạo tài liệu và đặt lịch họp.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @larksuite xem tin nhắn mới của mình."
- **Gọi nhanh**: gõ `@larksuite` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Lark Suite → đăng nhập/cấp quyền.

### Monday.com ⭐  (`monday`)

- **Dùng để làm gì**: Monday.com là nền tảng quản lý công việc nhiều kiểu xem. Với kết nối này, Agent có thể xem task, tạo item mới và cập nhật trạng thái trên từng board.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @monday xem các task giao cho mình."
- **Gọi nhanh**: gõ `@monday` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Monday.com → đăng nhập/cấp quyền.

### Trello ⭐  (`trello`)

- **Dùng để làm gì**: Trello là bảng quản lý công việc kiểu kéo-thả. Với kết nối này, Agent có thể tạo thẻ, di chuyển thẻ giữa các cột, liệt kê và xem thẻ được giao cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @trello xem các thẻ giao cho mình."
- **Gọi nhanh**: gõ `@trello` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Trello → đăng nhập/cấp quyền.

### Typeform ⭐  (`typeform`)

- **Dùng để làm gì**: Typeform là công cụ tạo form và khảo sát kiểu trò chuyện, hỏi từng câu một. Với kết nối này, Agent có thể tạo form, thu thập câu trả lời, xem phản hồi và xuất kết quả.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @typeform xem phản hồi mới từ form của mình."
- **Gọi nhanh**: gõ `@typeform` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Typeform → đăng nhập/cấp quyền.

### YouTube ⭐  (`youtube`)

- **Dùng để làm gì**: YouTube là nền tảng chia sẻ và xem video. Với kết nối này, Agent có thể xem video trên kênh, theo dõi lượt xem, đọc bình luận và tổng hợp chỉ số kênh cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @youtube xem các video mới đăng gần đây của kênh mình."
- **Gọi nhanh**: gõ `@youtube` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn YouTube → đăng nhập/cấp quyền.

### Lark Suite (Tenant)  (`larksuite-tenant`)

- **Dùng để làm gì**: Lark Suite (Tenant) là công cụ quản trị tenant Lark. Với kết nối này, Agent có thể liệt kê, thêm, xoá user và gán role admin theo phòng ban.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @larksuite-tenant liệt kê người dùng trong tenant."
- **Gọi nhanh**: gõ `@larksuite-tenant` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Lark Suite (Tenant) → đăng nhập/cấp quyền.

## Kỹ thuật

### Bitbucket ⭐  (`bitbucket`)

- **Dùng để làm gì**: Bitbucket là nền tảng quản lý mã nguồn Git của Atlassian. Với kết nối này, Agent có thể xem pull request chờ review, mở PR mới, tra commit và theo dõi repo của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @bitbucket xem các pull request đang chờ mình review."
- **Gọi nhanh**: gõ `@bitbucket` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Bitbucket → đăng nhập/cấp quyền.

### GitHub ⭐  (`github`)

- **Dùng để làm gì**: GitHub là nền tảng lưu trữ và quản lý mã nguồn cho cả nhóm. Với kết nối này, Agent có thể tạo issue, mở pull request, theo dõi review và tra commit cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @github xem các pull request đang chờ mình review."
- **Gọi nhanh**: gõ `@github` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn GitHub → đăng nhập/cấp quyền.

### GitLab ⭐  (`gitlab`)

- **Dùng để làm gì**: GitLab là nền tảng quản lý mã nguồn Git kèm CI/CD. Với kết nối này, Agent có thể xem merge request, mở MR mới, theo dõi review và tra pipeline cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @gitlab xem các merge request đang chờ mình review."
- **Gọi nhanh**: gõ `@gitlab` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn GitLab → đăng nhập/cấp quyền.

### Jira ⭐  (`jira`)

- **Dùng để làm gì**: Jira là công cụ quản lý issue và dự án. Với kết nối này, Agent có thể xem, tạo, gán và cập nhật issue qua sprint và epic.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @jira xem các task đang giao cho mình."
- **Gọi nhanh**: gõ `@jira` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Jira → đăng nhập/cấp quyền.

### Linear ⭐  (`linear`)

- **Dùng để làm gì**: Linear là công cụ quản lý task và bug gọn nhẹ. Với kết nối này, Agent có thể xem, tạo và cập nhật issue qua sprint và project.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @linear xem các task đang giao cho mình."
- **Gọi nhanh**: gõ `@linear` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Linear → đăng nhập/cấp quyền.

## Thương mại

### Shopify ⭐  (`shopify`)

- **Dùng để làm gì**: Shopify là nền tảng bán hàng online. Với kết nối này, Agent có thể xem đơn hàng, quản lý sản phẩm, tra tồn kho và cập nhật giá cho cửa hàng của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @shopify xem các đơn hàng gần đây."
- **Gọi nhanh**: gõ `@shopify` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Shopify → đăng nhập/cấp quyền.

### Stripe ⭐  (`stripe`)

- **Dùng để làm gì**: Stripe là nền tảng thanh toán trực tuyến. Với kết nối này, Agent có thể tạo link thu tiền, tra giao dịch, hoàn tiền và đối soát các khoản cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @stripe xem các khoản thanh toán gần đây."
- **Gọi nhanh**: gõ `@stripe` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Stripe → đăng nhập/cấp quyền.

### WooCommerce ⭐  (`woocommerce`)

- **Dùng để làm gì**: WooCommerce là plugin bán hàng cho website WordPress. Với kết nối này, Agent có thể xem đơn hàng, tra tồn kho, cập nhật sản phẩm và theo dõi vận chuyển.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @woocommerce xem các đơn hàng gần đây."
- **Gọi nhanh**: gõ `@woocommerce` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn WooCommerce → đăng nhập/cấp quyền.

## Hỗ trợ

### Freshdesk ⭐  (`freshdesk`)

- **Dùng để làm gì**: Freshdesk là phần mềm hỗ trợ khách hàng gộp mail, chat và điện thoại về một nơi. Với kết nối này, Agent có thể xem, trả lời, tóm tắt và sắp xếp ticket cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @freshdesk xem các ticket chưa giải quyết."
- **Gọi nhanh**: gõ `@freshdesk` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Freshdesk → đăng nhập/cấp quyền.

### Intercom ⭐  (`intercom`)

- **Dùng để làm gì**: Intercom là nền tảng chat hỗ trợ khách trên web và app. Với kết nối này, Agent có thể xem chat chưa trả lời, trả lời khách, gắn câu mẫu và mở đúng hội thoại cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @intercom xem các cuộc trò chuyện chưa trả lời."
- **Gọi nhanh**: gõ `@intercom` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Intercom → đăng nhập/cấp quyền.

### Zendesk ⭐  (`zendesk`)

- **Dùng để làm gì**: Zendesk là phần mềm hỗ trợ khách hàng. Với kết nối này, Agent có thể xem ticket ưu tiên, trả lời bằng câu mẫu, tóm tắt hội thoại và theo dõi ticket chưa xử lý.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @zendesk xem các ticket chưa giải quyết."
- **Gọi nhanh**: gõ `@zendesk` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Zendesk → đăng nhập/cấp quyền.

## Lưu trữ

### Box ⭐  (`box`)

- **Dùng để làm gì**: Box là dịch vụ lưu trữ đám mây cho doanh nghiệp. Với kết nối này, Agent có thể tìm tài liệu theo tên, chia sẻ link file, tải file lên và sắp xếp thư mục của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @box liệt kê file gần đây của mình."
- **Gọi nhanh**: gõ `@box` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Box → đăng nhập/cấp quyền.

### Dropbox ⭐  (`dropbox`)

- **Dùng để làm gì**: Dropbox là dịch vụ lưu trữ đám mây. Với kết nối này, Agent có thể tìm tài liệu theo tên, chia sẻ link file, tải file lên và sắp xếp thư mục của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @dropbox liệt kê file gần đây của mình."
- **Gọi nhanh**: gõ `@dropbox` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Dropbox → đăng nhập/cấp quyền.

### Google Drive ⭐  (`googledrive`)

- **Dùng để làm gì**: Google Drive là dịch vụ lưu trữ đám mây của Google. Với kết nối này, Agent có thể tìm file, chia sẻ link, tải file lên và sắp xếp thư mục cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @googledrive liệt kê file gần đây của mình."
- **Gọi nhanh**: gõ `@googledrive` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Google Drive → đăng nhập/cấp quyền.

### OneDrive ⭐  (`onedrive`)

- **Dùng để làm gì**: OneDrive là dịch vụ lưu trữ đám mây của Microsoft. Với kết nối này, Agent có thể tìm, tải lên, sắp xếp và chia sẻ file trong kho lưu trữ của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @onedrive liệt kê file gần đây của mình."
- **Gọi nhanh**: gõ `@onedrive` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn OneDrive → đăng nhập/cấp quyền.

## Lịch & họp

### Calendly ⭐  (`calendly`)

- **Dùng để làm gì**: Calendly là công cụ đặt lịch hẹn online. Với kết nối này, Agent có thể xem lịch hẹn sắp tới, tạo link đặt hẹn, dời và huỷ cuộc hẹn của bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @calendly xem các lịch hẹn sắp tới của mình."
- **Gọi nhanh**: gõ `@calendly` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Calendly → đăng nhập/cấp quyền.

### Google Calendar ⭐  (`googlecalendar`)

- **Dùng để làm gì**: Google Calendar là lịch online của Google. Với kết nối này, Agent có thể xem lịch, đặt cuộc họp, tìm khung giờ trống và mời người tham gia cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @googlecalendar xem lịch sắp tới của mình."
- **Gọi nhanh**: gõ `@googlecalendar` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Google Calendar → đăng nhập/cấp quyền.

### Outlook Calendar ⭐  (`outlookcalendar`)

- **Dùng để làm gì**: Outlook Calendar là lịch của Microsoft. Với kết nối này, Agent có thể đặt cuộc họp, xem lịch, kiểm tra khung giờ trống và mời người tham dự cho bạn.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @outlookcalendar xem lịch sắp tới của mình."
- **Gọi nhanh**: gõ `@outlookcalendar` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Outlook Calendar → đăng nhập/cấp quyền.

### Zoom ⭐  (`zoom`)

- **Dùng để làm gì**: Zoom là ứng dụng họp video online. Với kết nối này, Agent có thể tạo phòng họp, gửi link tham gia, lấy bản ghi và xem các cuộc họp sắp tới.
- **Ví dụ người dùng nói**: "Dùng tryopenclaw connectors @zoom xem các cuộc họp sắp tới của mình."
- **Gọi nhanh**: gõ `@zoom` trong câu nhắn.
- **Kết nối**: mở ClawExpert → mục Connectors → chọn Zoom → đăng nhập/cấp quyền.
